import TestScreen from '../../components/TestScreen';

export default function TabPerfilScreen() {
  return (
    <TestScreen
      title="Tabs — Perfil"
      description="Pestaña de perfil (icono persona)."
    />
  );
}
